<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Critical for NativeCode -> Termux Communication -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <permission
        android:name="com.ivarna.nativecode.permission.RUN_COMMAND"
        android:protectionLevel="dangerous"
        android:label="Run commands in FluxLinux terminal" />

    <queries>
        <package android:name="com.termux" />
        <package android:name="com.termux.x11" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NativeCode"
        tools:targetApi="35"
        tools:replace="android:allowBackup,android:roundIcon,android:theme">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.ivarna.nativecode.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/Theme.NativeCode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep Link for Script Callbacks -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nativecode" android:host="callback" />
            </intent-filter>
            <!-- Receive shared images -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <service
            android:name="com.ivarna.nativecode.core.termux.TerminalService"
            android:exported="false" />

        <service
            android:name="com.ivarna.nativecode.core.termux.CliService"
            android:exported="true"
            android:permission="com.ivarna.nativecode.permission.RUN_COMMAND">
            <intent-filter>
                <action android:name="com.ivarna.nativecode.RUN_COMMAND" />
            </intent-filter>
        </service>

        <!-- Termux X11 in-app display only — NOT a second launcher icon.
             Library module ships MAIN/LAUNCHER; tools:node=replace drops that so
             only com.ivarna.nativecode.MainActivity appears on the app list.
             (Two LAUNCHER activities in one package = two icons; uninstall removes both.) -->
        <activity
            android:name="com.termux.x11.MainActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustPan"
            android:excludeFromRecents="false"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            tools:node="replace" />

        <!-- Preferences: open only in-app; no MAIN/LAUNCHER -->
        <activity
            android:name="com.termux.x11.LoriePreferences"
            android:exported="false"
            android:launchMode="standard"
            android:taskAffinity=""
            android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
            android:parentActivityName="com.termux.x11.MainActivity"
            android:excludeFromRecents="true"
            tools:node="replace" />

    </application>

</manifest>

